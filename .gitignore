# dependencies (docs/ still builds VitePress with npm)
node_modules/

# build / compile
bin/herdr-workflows
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# env / secrets
.env
.env.*
!.env.example

# logs / OS
*.log
.DS_Store
Thumbs.db

# test / coverage / caches
coverage/
coverage.out
*.lcov
.cache/

# go workspaces
go.work
go.work.sum

# docs (vitepress)
docs/.vitepress/cache/
docs/.vitepress/dist/

# go-semantic-release downloaded plugins and local cache
.semrel/

# local scratch / agent harness
.plans/
.cursor/
.opencode/
.idea/
*.swp
*.swo
*~

.hwf/
.kimi/

# local Herdr and other reference checkouts (tracked instructions only)
.agents/references/**
!.agents/references/AGENTS.md

# leftover untracked clone path from older layouts
/references/

# playwright MCP session artifacts
/.playwright-mcp/
