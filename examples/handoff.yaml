version: v1alpha1
title: Handoff
description: Distil this session and hand it to another agent
inputs:
  target: profile
  focus:
    type: text
    default: ""
steps:
  - workflow: handoff-bg
    inputs:
      target: "{{inputs.target}}"
      focus: "{{inputs.focus}}"
